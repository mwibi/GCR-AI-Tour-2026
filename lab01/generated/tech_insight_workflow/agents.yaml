agents:
- name: LocalToolExecutorAgent
  instructions: You are a specialized assistant for a workflow step. Follow the prompt
    and return only what it requests.
- name: HotspotClusteringAgent
  instructions: You are a specialized assistant for a workflow step. Follow the prompt
    and return only what it requests.
- name: InsightAgent
  instructions: You are a specialized assistant for a workflow step. Follow the prompt
    and return only what it requests.
- name: ContentStrategyAgent
  instructions: You are a specialized assistant for a workflow step. Follow the prompt
    and return only what it requests.
