## Social Insight workflow (.env)
## 默认使用 Azure AI Foundry Project + Foundry Agent Service
##
## 认证：
## - 本地开发：先执行 `az login`（无需在此写 key/secret）
## - Azure 环境：推荐 Managed Identity（无需在此写 key/secret）

# 必填：AI Foundry Project endpoint
AZURE_AI_PROJECT_ENDPOINT=https://<your-foundry-resource>.services.ai.azure.com/api/projects/<your-project>

# 必填：Foundry Project 中的模型部署名
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-5-mini

# 可选：固定使用某个已存在的 Foundry Agent（否则 runner 可按 agent 名称自动解析/创建）
# AZURE_AI_AGENT_ID=<your-agent-id>
